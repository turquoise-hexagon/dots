#!/bin/sh
#
# mpd script

if test -s ~/.config/mpd/pid; then
    echo 'error : mpd already running'
    exit 1
fi

# start daemon
mpd 2> /dev/null

# send notification on mpd updates
while test -s ~/.config/mpd/pid; do
    var=$(mpc current --wait)
    notify "$var"
done
