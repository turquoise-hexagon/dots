prompt()
{
    local acc res var status=$?

    # colors
    res='\[\e[0m\]'

    case $status in
        0) acc='\[\e[95m\]';; # normal
        *) acc='\[\e[31m\]'   # misc error
    esac

    # path
    var=${PWD/$HOME/\~}

    # prompt
    PS1="$acc$var$res "
}

# vim: ft=sh
