#!/usr/bin/env bash
#
# rename picture based on exifs

set -e

if ! [[ $1 ]]; then
    exit
fi

# get extension
ext=${1##*.}
ext=${ext,,}

# get date from exifs
date=$(identify -format "%[EXIF:DateTime]" "$1")
date=${date//:/-}
date=${date// /_}

mv "$1" "${date}.${ext}"
