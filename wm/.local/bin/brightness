#!/bin/sh
#
# brightness - change brightness

MIN=30 # minimum brightness
DIR=/sys/class/backlight/intel_backlight

read -r cur < $DIR/brightness
read -r max < $DIR/max_brightness

var=$((cur $1 max * $2 / 100))

echo $((
    var > max ? max :
    var < MIN ? MIN : var
)) > $DIR/brightness
