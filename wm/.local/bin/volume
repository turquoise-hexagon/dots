#!/bin/sh
#
# volume - control volume

var=$(amixer set Master "$1")

test "$1" = toggle && exit

# send a notification when fullscreen
bspc query -N -n focused.fullscreen > /dev/null && {
    # format volume
    var=${var%%]*}
    var=${var#*[}

    dunstify -r 501 "Volume : $var"
}
