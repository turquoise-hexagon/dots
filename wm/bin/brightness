#!/bin/sh
#
# change brightness

# set minimum brightness
min=25

cur=$(brightnessctl g)
max=$(brightnessctl m)

var=$((cur $1 max * $2 / 100))

[ "$var" -gt "$max" ] && var=$max
[ "$var" -lt "$min" ] && var=$min

brightnessctl -q s "$var"
