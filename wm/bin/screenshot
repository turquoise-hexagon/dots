#!/bin/sh
#
# take a screenshot & send a notification
#
# wm key bindings

dir=~/pics/screenshots
var=$(date +%F_%H-%M-%S)

mkdir -p "$dir" || exit 1

maim -s "$dir/$var.png" &&
    notify-send "Saved screenshot as $var.png"
