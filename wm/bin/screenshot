#!/bin/sh
#
# take a screenshot & send a notification
#
# wm key bindings

dir="$HOME/pictures/screenshots"
var=$(date +%d-%m-%y_%H-%M-%S)

mkdir -p "$dir"

maim -s "$dir/$var.png" &&
    notify -m "Saved screenshot as $var.png"
