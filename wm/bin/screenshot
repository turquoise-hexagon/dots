#!/bin/sh
#
# take a screenshot & send a notification
#
# wm key bindings

dir=~/pics/screenshots
var=$(date +%F_%H-%M-%S)

mkdir -p "$dir"

maim -o -s "$dir/$var.png" &&
    notify "Saved screenshot as $var.png"
