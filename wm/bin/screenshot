#!/bin/sh
#
# take a screenshot & send a notification
#
# wm key bindings

dir=~/pics/screenshots
cur=$(date +%F_%H-%M-%S)

mkdir -p "$dir"

# disable compositor warning
maim -qsu "$dir/$cur.png" &&
    notify "Saved screenshot as $cur.png"
