#!/usr/bin/env dash
#
# remote volume control
#
# wm key bindings

vol() {
    amixer -q sset "$@"
}

case "$1" in
    mic) vol Capture toggle;;
    toggle) vol Master toggle;;
    up) vol Master 5%+;;
    down) vol Master 5%-;;
esac

# vim: ft=sh
