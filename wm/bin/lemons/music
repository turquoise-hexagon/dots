# get song
#
# lemonbar script

mus ()
    while :; do
        case $(mpc) in
            *playing*)
                out=$(mpc current)
                ;;
            *)
                out=Paused
        esac
        echo "M$a$t $out"

        mpc idle
    done

# vim: ft=sh
