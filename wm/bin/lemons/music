# get song
#
# lemonbar script

mus ()
    while :; do
        case $(mpc) in
            *playing*)
                var=$(mpc current)
                ;;
            *)
                var=Paused
        esac
        echo "M$a$t $var"

        mpc idle
    done

# vim: ft=sh
