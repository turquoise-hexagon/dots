# sourced by 'panel'

mus ()
    while :; do
        # pattern matching
        case $(mpc) in
            *paused*) var='Not playing';;
            *) var=$(mpc current)
        esac

        echo "M $var"

        mpc idle
    done

# vim: ft=sh
