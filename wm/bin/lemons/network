# sourced by 'panel'

ntw ()
    while :; do
        var=$(/sbin/iwgetid -r)

        # fallback to down
        var=${var:-Down}

        echo "N$a$t $var"

        sleep 2
    done

# vim: ft=sh
