#!/bin/sh
#
# remote mpc control
#
# wm key bindings

# exit if mpd isn't running
[ -s ~/.config/mpd/pid ] || exit 1

mpc "$1"

if [ "$1" != toggle ]; then
    # send notification when fullscreen
    if bspc query -N -n .local.fullscreen > /dev/null; then
        notify "$(mpc current)"
    fi
fi
