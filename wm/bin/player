#!/bin/sh
#
# remote cmus control
#
# wm key bindings

pgrep -x cmus > /dev/null && {
    case $1 in
        next) cmus-remote -n;;
        prev) cmus-remote -r;;
        play) cmus-remote -u
    esac
}
