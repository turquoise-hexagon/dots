mus()
    while :; do
        case $(mpc) in
            *playing*) var=$(mpc current);;
            *)         var='Not Playing'
        esac

        echo "M $var"

        mpc idle player
    done

# vim: ft=sh
