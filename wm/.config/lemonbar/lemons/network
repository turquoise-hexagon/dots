#!/bin/sh

readonly DEV=wlp59s0

# ensure network block isn't empty on startup
{ echo; ip monitor route dev "$DEV"; } |
    while read _; do
        var=$(iwgetid -r | sed 's|%|%%|g')
        var=${var:-Disconnected}

        printf '%s\n' "ntw %{O10}$var"
    done
