#!/bin/sh
#
# mpv - small mpv wrapper

# pause other players
mpc pause > /dev/null

playerctl -l 2> /dev/null |
    while read -r line; do
        playerctl -p "$line" pause
    done

command -p mpv "$@"
