#!/usr/bin/env bash
#
# rename picture based on exifs

set -e

! [[ $1 ]] && exit

# get extension
ext=${1##*.}
ext=${ext,,}

# get date from exifs
date=$(identify -format "%[EXIF:DateTime]" "$1")
date=${date//:/-}
date=${date// /_}

mv "$1" "${date}.${ext}"
