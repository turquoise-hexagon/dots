#!/bin/sh
#
# mount my sdcard

mnt="/mnt/sdcard"
dev="/dev/mmcblk0p1"

check() {
    [ ! -e "$1" ] &&
        { echo "error : '$1' not found"; exit 1; }
}

check "$mnt"
check "$dev"

sudo mount "$dev" "$mnt"
