#!/usr/bin/env bash
#
# mount sdcard

mnt="/mnt/sdcard"
dev="/dev/mmcblk0p1"

check() {
    ! [[ -e $1 ]] &&
        { echo "ERROR : '$1' not found"; exit; }
}

check "$mnt"
check "$dev"

sudo mount "$dev" "$mnt" &&
    echo "done !"
