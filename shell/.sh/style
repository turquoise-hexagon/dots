prompt() {
    local acc res var status=$?

    res='\[\e[0m\]' # reset colors

    case $status in
        0) acc='\[\e[95m\]';; # normal
        *) acc='\[\e[31m\]'   # misc error
    esac

    var='\W' # short path

    # set prompt
    PS1="$acc$var$res "
}

PROMPT_COMMAND=prompt

# vim: ft=sh
