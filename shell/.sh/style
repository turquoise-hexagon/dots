prompt() {
    local acc res var ret=$?

    res='\[\e[0m\]' # reset colors

    case $ret in
        0) acc='\[\e[95m\]';; # normal
        *) acc='\[\e[31m\]'   # misc error
    esac

    var='\w' # short path

    # set prompt
    PS1="$acc$var$res "
}

PROMPT_COMMAND=prompt

# vim: ft=sh
