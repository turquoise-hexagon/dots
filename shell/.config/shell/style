prompt() {
    local acc res var ret=$?

    res='\[\e[m\]'

    case $ret in
        0) acc='\[\e[91m\]';;
        *) acc='\[\e[36m\]'
    esac

    var='\w' # short path

    PS1="$acc$var$res "
}

PROMPT_COMMAND=prompt

# vim: ft=sh
