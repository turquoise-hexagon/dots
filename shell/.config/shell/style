prompt() {
    local acc res var ret=$?

    res='\[\e[0m\]' # reset colors

    case $ret in
        0) acc='\[\e[91m\]';; # normal
        *) acc='\[\e[93m\]'   # misc error
    esac

    var='\w' # short path

    PS1="$acc$var$res "
}

PROMPT_COMMAND=prompt

# vim: ft=sh
