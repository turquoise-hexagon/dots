set mouse=true

set format_current= %a %= %l
set format_statusline= %{status} %{?d?%t %= %{position} / %d}
set format_playlist= %-25%a %n. %t %= %y %d

set status_display_program=cmus-status

colorscheme cherry

# vim: ft=cfg
