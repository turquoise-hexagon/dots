#!/usr/bin/env bash
#
# send notification on cmus update
#
# cmus script

# parse datas from cmus
while [[ $# -gt 0 ]]; do
    printf -v "$1" "%s" "$2"

    shift
    shift
done

[[ $status == playing ]] &&
    notify "$artist - $title"
