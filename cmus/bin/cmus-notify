#!/usr/bin/env dash
# shellcheck disable=SC2154
#
# send a notification on cmus changes
#
# cmus tool

while [ "$#" -ge 2 ]; do
    eval "_$1"='$2'
    shift
    shift
done

if [ "$_status" != paused ]; then
    notify-send -t 1000 "$_title" "$_artist - $_album"
fi

# vim: ft=sh
