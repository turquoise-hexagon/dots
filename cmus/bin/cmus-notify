#!/bin/sh
#
# send a notification on cmus update
#
# cmus tool

while [ "$#" -gt 2 ]; do
    case "$1" in
        title) title="$2";;
        status) status="$2";;
    esac
    shift
    shift
done

if [ "$status" = playing ]; then
    # use notify script
    notify -t 3 "$title"
fi
