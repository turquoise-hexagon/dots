#!/bin/sh
#
# watch for window events
#
# wmutils

wew | while IFS=: read -r ev id; do
    wattr o "$id" && continue
    case "$ev" in
        18) focus prev "$id";;
        19) focus "$id";;
        16) tp 5 "$id";;
    esac
done
