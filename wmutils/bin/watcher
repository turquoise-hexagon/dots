#!/bin/sh
#
# watch for window events
#
# wmutils

wew | while IFS=: read -r ev id; do
    case "$ev" in
        18) wattr o "$id" || focus "$(lsw | head -n 1)";;
        19) wattr o "$id" || focus "$id";;
        16) wattr o "$id" || tp 5 "$id";;
    esac
done
