#!/bin/sh
#
# mpd script

# start daemon
mpd 2> /dev/null

# subscribe to mpd events
# send notifications on events
mpc idleloop player | \
    while read -r event; do
        var=$(mpc current --wait)
        notify "$var"
    done
